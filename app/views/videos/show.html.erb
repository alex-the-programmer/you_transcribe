<% description @video.name %>
<% title @video.name %>
<div class="page">
    <div class="channel">
        <div> 
            <img src=<%= @video.channel.logo_url %> alt="Channel Logo URL" class="channel-logo" >
        </div>
        <h1><%= link_to @video.channel.name, @video.channel.url %> </h1>
        <%# <div>
            <input placeholder="Search on the channel...">
        </div> %>
    </div>
    <iframe width="560" height="315" src="<%= @video.embed_url %>" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    <h2 class="video-name"><%= @video.name %></h2>
    <div class="transcription">
        <div>
            <% @video.transcription.each do |transcription_line| %>
                <p> 
                    <span>
                        <%= transcription_line['offset'] %>
                    </span>
                    <%= transcription_line['text'] %>
                </p>
            <%end%>
        </div>
    </div>
</div>